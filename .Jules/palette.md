## 2025-05-14 - [Mobile Navigation Accessibility]
**Learning:** Adding mobile navigation requires careful attention to ARIA labels and state management. Using Next.js `Link` for mobile menus ensures client-side transitions and better UX compared to standard `<a>` tags.
**Action:** Always use `<Link>` for internal navigation and provide clear `aria-expanded` and `aria-label` for toggle buttons.
